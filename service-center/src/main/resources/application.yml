spring:
  application:
    name: service-center

  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 50MB
  jackson:
    serialization:
      write-dates-as-timestamps: false

springdoc:
  default-produces-media-type: application/json

server:
  servlet:
    context-path: /api/v1/