package com.maintainance.service_center.quoterequest;

// Spec 008/009 — how the customer would prefer the service delivered (hint only).
public enum FulfillmentHint {
    DROP_OFF,
    PICKUP_DELIVERY,
    AT_HOME
}
